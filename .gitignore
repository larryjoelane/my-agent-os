# Scratch/test files, not part of the repo's tracked content
/tmp/

# memory graph build output (graph.js --out _site); Pages builds it in CI
_site/

# memory skill's local data store (created wherever recall.js/recall-store.js
# are run from — see skills/memory/SKILL.md)
.myagent/

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/
.npm/
.yarn/
.yarn-integrity
package-lock.json.bak
*.tsbuildinfo
.eslintcache
.node_repl_history
.env
.env.local
.env.*.local
coverage/
.nyc_output/
dist/
build/
out/
.cache/
